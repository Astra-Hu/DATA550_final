packages <- c(
"tidyverse",
"janitor",
"broom",
"knitr",
"kableExtra",
"here",
"rmarkdown"
)
# 安装还没有安装的包
installed <- rownames(installed.packages())
for (p in packages) {
if (!(p %in% installed)) {
install.packages(p)
}
}
getwd()
